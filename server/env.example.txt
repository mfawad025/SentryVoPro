# Copy this file to .env and fill in real values. Never commit the real .env file.

# ---- Lemon Squeezy ----
# 1. Create two products in your Lemon Squeezy dashboard:
#      "Single Keyword/Account"      — $50/month
#      "Multiple Keywords/Accounts"  — $100/month
# 2. On each product, click Share and copy its Checkout Link below.
LEMONSQUEEZY_CHECKOUT_URL_SINGLE=https://mfawad442.lemonsqueezy.com/checkout/buy/2c9cc3d9-71d8-40d3-b4d2-4846ecc68da5
LEMONSQUEEZY_CHECKOUT_URL_MULTI=https://mfawad442.lemonsqueezy.com/checkout/buy/c3cbc4f6-b2d6-4750-af68-ae6a1a997ace
# 3. Dashboard > Settings > Webhooks > add https://sentryvopro.onrender.com/api/lemonsqueezy/webhook
#    subscribed to at least: order_created, subscription_created
#    Copy the signing secret it gives you:
LEMONSQUEEZY_WEBHOOK_SECRET=sentryvo442
# 4. First-time-customer discount ($50→$30, $100→$80 — a flat $20 off works
#    for both plans). Dashboard > Discounts > New Discount, $20 off, restrict
#    to "New customers only" so Lemon Squeezy enforces one-time-use itself.
#    Put the discount CODE (not the ID) here:
LEMONSQUEEZY_FIRST_ORDER_DISCOUNT_CODE=EYMJMYOQ

# ---- Google Custom Search (scanning source — used for both web + image search) ----
# 1. Get an API key: https://console.cloud.google.com/apis/credentials (enable "Custom Search API")
# 2. Create a search engine set to "Search the entire web": https://programmablesearchengine.google.com/
# 3. Copy its Search Engine ID (cx) below
GOOGLE_CSE_API_KEY=AIzaSyDWgGzAMjBsC1286jsrQI94AsGDUY3d4Lw
GOOGLE_CSE_CX=c02ac4a3cc7154351

# ---- Email (for leak reports) ----
# Any SMTP provider works (Gmail app password, Brevo, SendGrid, Mailgun, etc.)
SMTP_HOST=smtp-relay.brevo.com
SMTP_PORT=587
SMTP_USER=b56745001@smtp-brevo.com
SMTP_PASS=xsmtpsib-567768e4edeabaadce71c4a0445481de76ea85fe597b4d5e9c83f7476758ef6e-ggKvD3BrIxkd2Dfq
REPORT_FROM_EMAIL="SentryVo Reports <mfawad442@gmail.com>"

# ---- Free scan (public homepage tool) ----
# Site-wide daily cap across ALL visitors, to protect the Google API quota
# your paying subscribers' daily scans depend on. Each visitor is separately
# capped at 3/day regardless of this setting (see index.js).
FREE_SCAN_DAILY_SITE_CAP=25

# ---- Scheduling ----
# The scan itself runs daily regardless of plan. Whether an EMAIL goes out
# that day depends on each user's report_frequency_days (auto-set: 1 day for
# the Multiple Keywords/Accounts plan, 3 days for Single Keyword/Account — see db.js).
DAILY_SCAN_CRON=0 8 * * *

# ---- Server ----
PORT=4242
ALLOWED_ORIGIN=https://yourdomain.com
DATABASE_FILE=./sentryvo.db
