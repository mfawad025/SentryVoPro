# Copy this file to .env and fill in real values. Never commit the real .env file.

# ---- Lemon Squeezy ----
# 1. Create two products in your Lemon Squeezy dashboard:
#      "Single Keyword/Account"      — $50/month
#      "Multiple Keywords/Accounts"  — $100/month
# 2. On each product, click Share and copy its Checkout Link below.
LEMONSQUEEZY_CHECKOUT_URL_SINGLE=https://yourstore.lemonsqueezy.com/checkout/buy/YOUR-SINGLE-VARIANT-UUID
LEMONSQUEEZY_CHECKOUT_URL_MULTI=https://yourstore.lemonsqueezy.com/checkout/buy/YOUR-MULTI-VARIANT-UUID
# Agency tiers — create these as two more products (or variants) in Lemon
# Squeezy: "Agency Starter" $200/mo, "Agency Growth" $500/mo. Enterprise has
# no checkout link — it routes to the Contact Sales form on agencies.html.
LEMONSQUEEZY_CHECKOUT_URL_AGENCY_STARTER=https://yourstore.lemonsqueezy.com/checkout/buy/YOUR-AGENCY-STARTER-UUID
LEMONSQUEEZY_CHECKOUT_URL_AGENCY_GROWTH=https://yourstore.lemonsqueezy.com/checkout/buy/YOUR-AGENCY-GROWTH-UUID
# 3. Dashboard > Settings > Webhooks > add https://<your-backend>/api/lemonsqueezy/webhook
#    subscribed to at least: order_created, subscription_created
#    Copy the signing secret it gives you:
LEMONSQUEEZY_WEBHOOK_SECRET=your_webhook_signing_secret
# 4. First-time-customer discount — 40% off first payment on BOTH plans
#    ($50→$30, $100→$60). Since it's the same percentage for both, one
#    discount code covers everything:
#      Dashboard > Discounts > New Discount
#        - "FIRST40": 40% off, store-wide (all products), restricted to
#          "New customers only"
#    Put the discount's CODE (not its internal ID) here:
LEMONSQUEEZY_FIRST_ORDER_DISCOUNT_CODE=your_discount_code

# ---- Serper.dev (scanning source — used for both web + image search) ----
# 1. Sign up at https://serper.dev (no card required — 2,500 free queries
#    on signup, then roughly $1 per 1,000 queries)
# 2. Copy your API key from the dashboard
SERPER_API_KEY=your_serper_api_key

# ---- Email (for leak reports) ----
# Any SMTP provider works (Gmail app password, Brevo, SendGrid, Mailgun, etc.)
SMTP_HOST=smtp.yourprovider.com
SMTP_PORT=587
SMTP_USER=your_smtp_username
SMTP_PASS=your_smtp_password
REPORT_FROM_EMAIL="SentryVo Reports <reports@sentryvo.com>"

# ---- DMCA agent identity ----
# Required legal elements on every automated takedown notice: the filer's
# real name, a real physical address, and a valid email — DMCA notices sent
# without these can be deemed non-compliant. This is filed as SentryVo's
# authorized-agent identity (acting on behalf of each individual creator),
# not each creator's own name — that's the correct, legally accurate way
# for a service like this to sign notices it files for its subscribers.
DMCA_AGENT_NAME="Muhammad Fawad"
DMCA_AGENT_ADDRESS="Your full physical address here"
DMCA_AGENT_EMAIL=support@sentryvo.com

# ---- Free scan (public homepage tool) ----
# Site-wide daily cap across ALL visitors, to protect the Serper.dev quota
# your paying subscribers' daily scans depend on. Each visitor is separately
# capped at 3/day regardless of this setting (see index.js).
FREE_SCAN_DAILY_SITE_CAP=25

# ---- Emergency admin access ----
# Protects /api/admin/find-user and /api/admin/deactivate-user — pick any
# long random string yourself (e.g. mash the keyboard for 20+ characters).
# Treat this like a password: never share it, never commit a real value.
ADMIN_SECRET_KEY=change_me_to_something_random

# ---- Site URL (used to redirect buyers to success.html after checkout) ----
# Also set this SAME url + /success.html as the "Redirect URL" directly in
# Lemon Squeezy's dashboard (each Product > Checkout tab) as a fallback —
# that's the setting Lemon Squeezy is guaranteed to honor.
SENTRYVO_SITE_URL=https://www.sentryvo.com

# ---- Scheduling ----
# The scan itself runs daily regardless of plan. Whether an EMAIL goes out
# that day depends on each user's report_frequency_days (auto-set: 1 day for
# the Multiple Keywords/Accounts plan, 3 days for Single Keyword/Account — see db.js).
DAILY_SCAN_CRON=0 8 * * *

# ---- Server ----
PORT=4242
# Must be your EXACT frontend domain (not *) — login now uses cookies,
# and cookie-based CORS requires a specific origin, not a wildcard.
ALLOWED_ORIGIN=https://www.sentryvo.com
# ---- Database (Postgres — Neon recommended, free tier never expires) ----
# 1. Sign up at neon.tech, create a project
# 2. Copy the connection string it gives you (starts with postgresql://)
DATABASE_URL=postgresql://user:password@host/dbname?sslmode=require
